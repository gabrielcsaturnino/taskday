# TaskDay Environment Variables Example
# Copy this file to .env and fill with your actual values

# Email Configuration (for notifications)
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Database Configuration (if not using Docker)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/taskdaydb
SPRING_DATASOURCE_USERNAME=gabriel
SPRING_DATASOURCE_PASSWORD=123

# JWT Configuration
JWT_PRIVATE_KEY=classpath:private-key.pem
JWT_PUBLIC_KEY=classpath:public-key.pem
JWT_EXPIRATION=86400000

# Application Configuration
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

